// Function of M Raised to the Power N

//Only works whenvariable declared as 1
var result = "1" ;
//Only works whenvariable declared as 1
var m = "1";

function RaisedToThePower(2 , 2 ){

for ( var i = 1 ; i <= n ; i ++ ) {

  result = m * result;
}

return result
}

RaisedToThePower(2 , 2 );
